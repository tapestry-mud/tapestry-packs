properties:
  dialogue:
    description: "Dialogue script reference for NPC conversations"
    type: string
    applies_to: [npc]

  # The swell_* dial properties were graduated to @tapestry/core (0.1.21) so any pack can build
  # a swell boss. The swell-warden mob in this pack uses the core-declared dials and the
  # core-registered telegraph-rung validator + sidestep/brace counters.
