tags:
  item:
    description: "Identifies entity as a pickupable/interactable item"
    applies_to: [item]
  container:
    description: "Can hold other items via inventory system"
    applies_to: [item]
  fillable:
    description: "Can hold liquid, filled from fill_source entities"
    applies_to: [item]
  consumable:
    description: "Destroyed on use (eating, drinking, applying)"
    applies_to: [item]
  equippable:
    description: "Can be worn or wielded in an equipment slot"
    applies_to: [item]
  no_sell:
    description: "Cannot be sold to shops"
    applies_to: [item]
  currency:
    description: "Treated as currency in economic transactions"
    applies_to: [item]
  drinkable:
    description: "Can be drunk from directly (fountains, wells)"
    applies_to: [item]
  emits_light:
    description: "Emits light, improving visibility in dark rooms"
    applies_to: [item]
  shop:
    description: "NPC operates as a vendor with shop_sells inventory"
    applies_to: [npc]
  no_wander:
    description: "Mobs will not wander into this room"
    applies_to: [room]
  recall_point:
    description: "Valid teleport/recall destination"
    applies_to: [room]
  alignment_good:
    description: "Entity's alignment is in the good range"
    applies_to: [npc, player]
  alignment_neutral:
    description: "Entity's alignment is in the neutral range"
    applies_to: [npc, player]
  alignment_evil:
    description: "Entity's alignment is in the evil range"
    applies_to: [npc, player]
  no_heal:
    description: "Prevents healing from spells and potions; suppresses active/incoming healing only (not passive regeneration)"
    applies_to: [npc, player]
  readable:
    description: "Item can be read to display text content"
    applies_to: [item]
