name: "@tapestry/core"
version: "0.1.25"
type: core
display_name: "Tapestry Core"
description: "Engine foundation: recall room, combat, abilities, commands, progression, mob behaviors, socials, and all gameplay systems. Content packs layer areas, races, and classes on top."
author: "Tapestry"
license: "AGPL-3.0"
# Engine floor is documentation-only (the key does not bind). >=0.1.48 is where the
# sendRoomDescription binding gained the optional brief arg; on older engines the
# two-arg movement call still renders (extra args ignored) but brief has no effect.
engine: ">=0.1.48"
validation: strict
active: true
load_order: 0

content:
  weather_zones: "areas/weather_zones.yaml"
  area_definitions: "areas/**/area.yaml"
  rooms: "areas/**/rooms/*.yaml"
  items: "areas/**/items/*.yaml"
  equipment_slots: "equipment_slots.yaml"
  scripts: "dist/scripts/**/*.js"
  scripts_format: esm
  strings: "strings/**/*.yaml"
  help: "help/**/*.yaml"
  motd: "data/motd.txt"
  motd_color: "data/motd_color.txt"
